import { LoginScreen } from "@/components/screens/LoginScreen";

/* Figma V2 — Student Login · Invalid Password. light 1:12051 · dark 1:12384. frame 376×812. */
export default function LoginInvalidPasswordPage() {
  return <LoginScreen state="invalid-password" />;
}
