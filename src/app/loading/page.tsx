import { HomeScreen } from "@/components/screens/HomeScreen";

/* Figma V2 — Loading (1:10348 light / 1:10665 dark): Home with the Live Class
 * tile in a loading state (centered circular spinner). */
export default function LoadingPage() {
  return <HomeScreen loading />;
}
