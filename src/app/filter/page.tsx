import { FilterScreen } from "@/components/screens/FilterScreen";

/* Figma V2 — Filter, collapsed (Past Class + Filter button). light 1:30151 · dark 1:30255. frame 376×1099. */
export default function FilterPage() {
  return <FilterScreen />;
}
