import { FilterScreen } from "@/components/screens/FilterScreen";

/* Figma V2 — Filter, expanded (Filter button + 4 dropdowns). light 1:30195 · dark 1:30298. frame 376×1265. */
export default function FilterExpandedPage() {
  return <FilterScreen expanded />;
}
