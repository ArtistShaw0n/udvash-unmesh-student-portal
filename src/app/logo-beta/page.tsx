import { LogoBetaScreen } from "@/components/screens/LogoBetaScreen";

/* Figma V2 — Logo (Beta): header (logo + Beta) + empty body + footer. light 1:28864 · dark 1:28868. frame 376×812. */
export default function LogoBetaPage() {
  return <LogoBetaScreen />;
}
