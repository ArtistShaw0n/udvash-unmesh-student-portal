import { AuthCongratsScreen } from "@/components/screens/AuthCongratsScreen";

/* Figma V2 — Registration · Congratulations (registration completed). light 1:15844. frame 376×812. */
export default function RegisterDonePage() {
  return <AuthCongratsScreen variant="registered" />;
}
