import { TimestampsScreen } from "@/components/screens/TimestampsScreen";

/* Figma V2 — Timestamps, expanded (full 6-row Timestamps list). light 1:33977 · dark 1:34134. frame 376×1202. */
export default function TimestampsExpandedPage() {
  return <TimestampsScreen expanded />;
}
