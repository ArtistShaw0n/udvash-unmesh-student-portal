import { TimestampsScreen } from "@/components/screens/TimestampsScreen";

/* Figma V2 — Timestamps, collapsed (Video/Download player + Timestamps accordion). light 1:34286 · dark 1:34444. frame 376×1007. */
export default function TimestampsPage() {
  return <TimestampsScreen />;
}
