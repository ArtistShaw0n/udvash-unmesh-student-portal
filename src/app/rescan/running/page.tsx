import { RescanScreen } from "@/components/screens/RescanScreen";

/* Figma V2 — Rescan · Course & Content, running (countdown + View Question/Scan OMR). light 1:28923 · dark 1:28907. frame 376×812. */
export default function RescanRunningPage() {
  return <RescanScreen running />;
}
