import { RescanScreen } from "@/components/screens/RescanScreen";

/* Figma V2 — Rescan · Course & Content (exam info + Start/Rescan). light 1:28872 · dark 1:28890. frame 376×812. */
export default function RescanPage() {
  return <RescanScreen />;
}
