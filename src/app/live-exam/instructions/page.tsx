import { LiveExamInstructions } from "@/components/screens/LiveExamInstructions";

/* Figma V2 — Live Exam instructions, no version chosen (Star buttons disabled).
 * light 1:9077 · dark 1:9105. */
export default function LiveExamInstructionsPage() {
  return <LiveExamInstructions />;
}
