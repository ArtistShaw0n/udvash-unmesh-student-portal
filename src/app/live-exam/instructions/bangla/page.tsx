import { LiveExamInstructions } from "@/components/screens/LiveExamInstructions";

/* Figma V2 — Live Exam instructions, Bangla version chosen (Star buttons enabled).
 * light 1:9155 · dark 1:9130. */
export default function LiveExamInstructionsBanglaPage() {
  return <LiveExamInstructions selected />;
}
